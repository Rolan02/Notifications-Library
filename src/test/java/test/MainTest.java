package test;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertFalse;

/**
 * MainTest as required by the challenge specifications.
 * Contains a single test with assertFalse.
 */
class MainTest {

    @Test
    void testMain() {
        assertFalse(false);
    }
}
